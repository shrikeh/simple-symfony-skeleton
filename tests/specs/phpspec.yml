---
suites:
  app:
    spec_path: "%paths.config%/src"
    src_path: "%paths.config%/../../application/src"
    namespace: App
    spec_prefix: app
    composer_suite_detection: true
  lib:
    spec_path: "%paths.config%/src"
    src_path: "%paths.config%/../../application/src"
    namespace: App
    spec_prefix: app
    composer_suite_detection: true