<?php

declare(strict_types=1);

/**
 * @author {{ author.name }} <{{ author.email }}>
 * @copyright {{ copyright_date }}. All rights reserved.
 * @license MIT.
 */

namespace {{ test_case.namespace }};

use PHPUnit\Framework\TestCase;
use {{ test_case.subject.fqn }};

final class {{ test_case.class_name }} extends TestCase
{
    /**
    * @test
    */
    public function itIsAClass(): void
    {
        // arrange
        $subject = new {{ test_case.subject.class_name }}();
        // act

        // assert
        $this->assertInstanceOf({{ test_case.subject.class_name }}::class, $subject);
    }
}